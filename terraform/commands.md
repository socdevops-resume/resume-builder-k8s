
# Terraform/OpenTofu Commands
To initialize and apply the Terraform configuration, use the following commands:
```
tofu fmt
tofu init
tofu validate
tofu plan -out=myplan.tfplan
tofu apply myplan.tfplan
```